@model PruebaTec02KDSB.Models.Medida

@{
    ViewData["Title"] = "Details";
}

<h1>Detalle</h1>

<div>
    <h4>Medida</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Medida1)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Medida1)
        </dd>
    </dl>
</div>
<div>
    <a class="btn btn-primary btn-ls" asp-action="Edit" asp-route-id="@Model?.Id">Editar</a> |
    <a class="btn btn-danger btn-ls" asp-action="Index">Cancelar</a>
</div>
